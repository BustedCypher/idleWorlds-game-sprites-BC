#!/usr/bin/env node
'use strict';const fs=require('node:fs'),vm=require('node:vm'),path=require('node:path');const target=path.resolve(process.argv[2]||path.join(__dirname,'..','index.html')),html=fs.readFileSync(target,'utf8'),re=/<script\b([^>]*)>([\s\S]*?)<\/script>/gi;let m,tags=0,inline=0,json=0,errors=[];while((m=re.exec(html))){tags++;if(/\bsrc\s*=/.test(m[1]))continue;if(/type\s*=\s*["']application\/json/i.test(m[1])){json++;continue;}inline++;try{new vm.Script(m[2],{filename:target+':inline-'+tags});}catch(e){errors.push({script:tags,message:String(e.message||e)});}}console.log(JSON.stringify({scriptTags:tags,inlineJavaScript:inline,jsonScripts:json,parseErrors:errors}));process.exitCode=errors.length?1:0;
